// src\types.ts

interface CustomerSubmission {
  timestamp: string;
  consultantName: string;
  type: string;
  customerDocument: string;
  customerName: string;
  date: string;
  products: string;
  taxRegime: string;
  documents: string;
}
